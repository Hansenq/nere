extends layout

block content
  // Navbar
  .navbar.navbar-fixed-top
    .navbar-inner
      .container
        a.brand(href='#') #{title}
        ul.nav
          li.active
            a(href='#') Workspace
          li
            a(href='#aboutModal', data-toggle='modal') About
          li
            a(href='#contactModal', data-toggle='modal') Contact Us

  // "About" Modal
  #aboutModal.modal.hide.fade(tabindex='-1', role='dialog')
    .modal-header
        button.close(type='button', data-dismiss='modal') ×
        h3 About
      .modal-body
        p Nere is an experimenta
      .modal-footer
        button.btn(data-dismiss='modal') Close

  // "Contact Us" Modal
  #contactModal.modal.hide.fade(tabindex='-1', role='dialog')
    .modal-header
        button.close(type='button', data-dismiss='modal') ×
        h3 Contact Us
      .modal-body
        p Questions or comments? Found a bug? We'd love to hear from you.
        p Shoot us an email at rzhou186@stanford.edu. We'll get back to you ASAP!
      .modal-footer
        button.btn(data-dismiss='modal') Close

  // Infobar
  .infobar
    strong(style='float: right;') Lobby #{ipaddress}

  // Sidebar (client name, file sender, & nearby users)
  .sidebar
    .self-block
      strong You:
      &nbsp;&nbsp;
      input(type='text', value='')
    .file-sender
      button.btn.btn-primary.btn-large.btn-block Send New File
    .title-block
      strong People Near You
      &nbsp;
      i.icon-globe
    .users

  // Posts and file uploads
  .main
    .main-block
      .posts-container

  // Messenger
  .messenger
    .chat-sender
      input(type='text', placeholder='What is on your mind?')

// Init JS
block footerJs
  script
    var socket = io.connect(window.location.hostname);
    socket.emit('Initialize IP', !{JSON.stringify(ipaddress)});
